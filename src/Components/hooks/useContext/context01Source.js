import React from "react";



/**
 * use initialState here only if not using Context01.Provider in react component tree
 * (This default value can be helpful for testing components in isolation without wrapping them.)
 * const Context01 = React.createContext(initialStateXY);
 * https://reactjs.org/docs/context.html#reactcreatecontext
 */

const Context01 = React.createContext();
export default Context01;